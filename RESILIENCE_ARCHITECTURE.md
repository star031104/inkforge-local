# 自动导演可靠性架构

本文是 v0.14.0 起自动导演的故障处理与恢复契约。修改生成流程时，必须同时维护这里、自动测试和界面状态。

## 一、处理原则

自动导演把每一次模型结果分为三类：

| 类别 | 例子 | 系统动作 |
|---|---|---|
| 硬结构/连接故障 | llama.cpp 持续断连或超时；四轮都没有闭合 JSON；必填字段始终缺失 | 安全暂停，保存 `failure_kind=hard`、精确错误和最后检查点 |
| 软规划质量问题 | 相邻目标近似、岗位偏离、提前兑现卷末、古代题材直接使用现代技术词 | 四轮定向改写；改变事件与状态，不做同义改写；仍未完全通过则选差异度最高的完整候选并记录 `planning_debts` |
| 正文质量问题 | 连续性、长度、重复、人物状态或文风审计未达阈值 | 在配置次数内最小修订；无人值守模式记录 `quality_debts` 后继续，严格模式暂停 |

“继续”不等于忽略。所有降级都有可追踪债务；“暂停”也不等于重做，恢复只从最小未完成单元开始。

## 二、事务和检查点

1. 灵感开书：故事骨架 → 人物名单 → 每张人物卡 → 世界书。每个资产单独保存。
2. 全书规划：故事圣经 → 每卷因果契约 → 每卷剧情核心 → 每卷执行清单。每卷单独保存。
3. 分卷拆章：一次只生成一条章节路线，立即写入 `route_checkpoints[volume_id]`。
4. 逐章创作：单章执行计划 → 候选正文 → 审计/修订 → 记忆回灌 → 接纳正文。接纳后才推进 `chapter_index`。

应用意外退出时，数据库把仍在运行的任务标记为暂停。恢复时读取上述检查点，不依赖浏览器内存。

## 三、路线自愈

每条路线先检查完整字段，再依次检查：编号、标题、目标、冲突、转折、章末变化、卷末提前兑现、十二类章节岗位、历史题材时代语言。目标判重同时使用顺序相似度和中文二元词组覆盖率，避免通过换词绕过。

软质量失败后，下一轮提示会携带具体拒绝原因和被拒候选，要求模型改变具体事件、承担人物和状态维度。最多四轮。系统保留结构完整候选的差异度评分，避免第四轮偶然变差后丢掉此前更好的方案。

恢复旧任务时，系统会从第一条路线开始用当前规则复核。遇到首个问题后：

- 已通过的前缀不变；
- 后缀写入 `route_rejected_history`；
- 从首个问题章重新生成；
- 不覆盖已经接纳的正文。

## 四、质量债务

- `planning_debts`：路线软风险、AI 单章细化降级、本地记忆回灌。
- `quality_debts`：正文经过规定修订次数仍低于质量阈值。
- `route_rejected_history`：恢复复核时撤回的旧路线范围及原因。

同一阶段、卷和章节的规划债务会更新而不是无限重复；任务只保留最近 120 条规划债务和 30 次路线撤回记录。

## 五、恢复操作

1. 确认 llama.cpp 的 `/v1/models` 可访问。
2. 重启 `run.ps1`，等待后端 `/api/health` 的 schema 与前端一致。
3. 打开自动导演状态，阅读“当前检查点”和错误类别。
4. 点击“从检查点继续”。系统会先复核旧路线，再继续未完成小步。
5. 完稿后分别展开规划债务和正文质量债务复审。

不要通过删除数据库或重新创建作品处理普通失败；检查点设计就是为了保住已完成资产。

## 六、验收要求

- Python 编译和 JavaScript 语法检查通过。
- 全部 pytest 回归通过。
- 断连/残缺 JSON 最终安全暂停并显示硬故障。
- 路线第一次重复、第二次有效时自动恢复，不暂停。
- 四轮软质量失败时记录规划债务并继续。
- 旧检查点含违规路线时只截断坏路线及其后缀。
- 单章细化或记忆使用本地降级时，正文链路继续且界面可追踪。
