# InkForge Local 0.20.0 与《算尽苍生》100章最终工程验收报告

> 最终状态：**通过发布级工程验收**。100章正文、100章结构化记忆、100章 SiliconFlow 真实模型审计、正式数据库事务状态、全书确定性质量检查与自动化回归均已完成。

## 一、验收结论

本轮不是只生成一份长文本，而是完成并验证了长篇小说生产闭环：规划、逐章写作、正文接纳、真实模型审计、证据化长期记忆、历史章节返修、伏笔回收、全书导出、备份恢复和自动化测试。

最终结果：

- 正式正文：100/100章，均非空；
- 正文规模：规范化字符143,191；
- SiliconFlow 真实模型审计：100/100章通过，最终证据中0回退；
- 审计模型：`Qwen/Qwen3.5-4B`；
- 模型评分：最低90、最高95、平均92.91；
- 本地逐章质量：100/100章通过，最低100；
- 全书健康分：100/100；
- 重复章名、跨章完全重复段落、高相似章节、疲劳词、现代术语、分卷倒退、记忆完整性问题：均为0；
- 正式章节记忆状态：100/100为 `committed`；
- 记忆提交记录：104条，104条全部 `committed`；
- 章节执行告警：0；
- 长期线索：15条，15条全部 `closed`；
- 正文与100份审定章节资产逐章完全一致，记忆重建前后正文 SHA-256 不变；
- 自动化测试：231项全部通过。

因此，《算尽苍生》已经是可阅读、可复审、可重复导出、可恢复的正式数据库终稿，不再是“等待模型审计”的候选稿。

## 二、长期记忆为什么能真正“不忘设定”

InkForge 不依赖聊天窗口自然记忆，也不把整本小说每次都塞回模型。它把“记住”拆成七种可审计对象：

1. **稳定设定层**：故事圣经、世界规则、作者意图、叙事规格和禁写项；
2. **人物当前态**：地点、处境、持有物、情绪和外观状态，并记录最后更新章号；
3. **人物知情账本**：谁在第几章、通过什么方式、凭哪句正文知道了什么；
4. **权威事实层**：事实正文、证据、来源章、置信度、有效期和替代关系；
5. **伏笔生命周期**：`open → progressing/deferred/ready → closed`，同时记录回收条件、利益相关者和真正知情者；
6. **时间线与动态关系**：每章事件、因果、参与者，以及人物关系的最新变化；
7. **检索投影层**：将已接纳、可核验证据投影进知识图谱和全文索引，只取本章相关的小块上下文。

每条人物、事实、线索、时间线和关系更新都必须带有同章正文证据；证据不在已接纳正文中时拒绝写入。模型的输出只是候选，SQLite 中经证据验证的状态才是权威记忆。

这实现了此前对 Grok 网页版、手机写作、goink、InkOS、NovelAI 和 agent 写作 skills 的核心结论：模型本身没有可靠的无限长期记忆，工程上必须建立外部状态、分层摘要、按需检索、知情边界、版本提交和回归检查。相关比较分析保留在 `INKFORGE_PROJECT_COMPARISON_AND_MEMORY_IMPLEMENTATION_REPORT_2026-08-29.md`，本文件记录最终落地结果。

## 三、章节生产与提交闭环

正式生产顺序如下：

1. 从故事圣经、分卷契约、章节路线和未结线索构建当章上下文；
2. 只检索当章相关的既有事实、人物知情、关系和时间线；
3. 生成或导入候选正文；
4. 本地检查长度、元话语、循环、重复、跨章复用、视角、时代语汇和未经支持的往事；
5. 真实模型审计人物知情、设定、因果、时间线、伏笔、路线交付和语言；
6. 审计通过后创建待提交记录并原子接纳正文；
7. 逐条核验记忆证据后回写人物、事实、线索、关系、时间线和知识图谱；
8. 将记忆提交标记为 `committed`；失败则保留正文并明确标记待恢复，不形成静默半提交；
9. 历史章节返修再次审计，但旧章记忆不得覆盖更晚章节形成的当前状态。

## 四、本轮发现并修复的关键缺陷

### 4.1 历史章节返修会倒退当前状态

第46、47、54章轻量措辞返修后，旧实现会重新应用旧章记忆，并把秦策、嬴政、王绾、李斯、赵明等人的当前状态倒退到旧章节；“急牍直达试法”和“咸阳之眼”也会从已关闭倒退为推进中。

修复内容：

- 人物增加 `last_state_chapter_number` 与来源章ID；
- 线索以 `last_advanced_chapter` 判断时间先后；
- 关系以 `last_chapter_number` 判断时间先后；
- 历史重放不得覆盖更晚人物状态、故事摘要、线索状态、关系状态和连续性备注；
- 提供仅供确定性全量重建使用的 `rebuild_current_projection=True`，按1—100章正序恢复当前态；
- 成功重放会清除旧执行告警，失败才保留告警。

修复后关键状态：秦策、嬴政、王绾、李斯和季禾停留在第100章；赵明停留在其最后更新的第69章；“急牍直达试法”关闭于第49章，“咸阳之眼”关闭于第100章。

### 4.2 关系字段兼容不一致

100份记忆资产主要使用 `from/to/change`，旧的记忆层和知识图谱层只读取 `left/right/state`，导致关系资产虽然存在，正式关系记忆却可能为0。

修复后两层同时兼容两套字段，并要求关系两端必须是已登记人物且证据可回指正文。正式结果为动态关系记忆35条、知识图谱关系43条；未知或机构型端点不再伪装成人物。

### 4.3 终局主题线索已写完但未关闭

“名籍之外的人”在第20章被设为长期延后，正文第100章已经通过“待属缓录”完整回答，但结构化状态没有关闭。现用第100章原文证据完成回收：无名病童可以领取药粮，又不被方便的姓名强行决定归属。最终15条长期线索全部关闭。

### 4.4 轻量整章返修被误判为抄袭自身

质量检查原来只排除完全相同文本。旧章只替换几个现代词后，其余原句会被误判为跨章复用。现在对长度接近且双向高度相似的整章返修排除自比较，同时仍能识别从长章复制短段的真实复用。

### 4.5 编辑状态、证据与未来信息

- 支持 `reopened`、`ready_for_payoff`、`resolved_with_cost`、`resolved_with_boundary`、`resolved_as_process` 等编辑状态并归一到标准生命周期；
- 100章人物、事实、线索、时间线和关系证据全部逐项回指同章正文；
- 删除第89章提前泄漏的第100章决策；
- 工作流警告不会进入小说世界记忆；
- 第46、47、54章的“版本”“概率”等现代感措辞已修订并分别通过95分真实审计。

## 五、真实模型审计结果

### 5.1 覆盖范围

- 第1—57章：采用严格时间顺序的只读发布复审；审第N章时只提供第1至N−1章及其证据化记忆，避免未来状态倒灌；57/57通过，最终记录0回退；
- 第58—100章：真实模型终审后逐章事务提交；43/43通过，0回退；
- 第46、47、54章：措辞返修后另做一次专项审计，3章均95分通过。

合并100章发布审计：最低90，最高95，平均92.91，100/100通过。

### 5.2 模型建议如何处理

100章审计共返回120条非阻断建议，其中中等50条、低级70条，没有高级问题。主要类别为连续性/一致性、风格/人物声音、重复、情节/逻辑、节奏和清晰度。35章带有建议，但模型最终均判定 `pass` 且不低于90分。

这些建议不能自动等同于已确认缺陷：部分是审计模型对正文隐含信息的保守疑问。发布门禁采用“三证合一”：真实模型结论、确定性检查和结构化记忆证据。当前没有同时触发三类证据的阻断问题，因此不继续盲目改写已经闭合的正文，以免为消除单次模型偏好而破坏已验证的因果或人物声音。120条原始建议完整保留在两个 JSON 审计文件中，可作为第二版人工精修清单。

## 六、正式数据库与记忆重建结果

最终正式投影包含：人物38名、权威事实511条、时间线事件100条、长期线索15条（全部关闭）、动态关系35条、知识图谱事实1100条、知识图谱关系43条。记忆重放告警和章节执行告警均为0，正文哈希重建前后完全不变，项目设置中 API 密钥为空。

正式重建前备份为 `data/backups/inkforge-20260903-135555-555263.db`。重建报告为 `artifacts/qince-formal-memory-rebuild.json`。

## 七、工程文件说明

### 核心运行层

- `app/main.py`：API、导演流程、接纳事务、证据验证、人物/事实/线索/时间线/关系回写，以及历史记忆时间顺序保护；
- `app/db.py`：SQLite 项目存储、版本记录、章节版本、备份、默认结构与安全字段；
- `app/knowledge.py`：权威实体、事实与关系图谱投影，现兼容两套关系字段；
- `app/memory.py`、`app/memory_integrity.py`：记忆检索、摘要和完整性检查；
- `app/canon.py`、`app/references.py`：故事圣经和参考材料权威边界；
- `app/planning.py`、`app/prompts.py`：分卷契约、章节路线和上下文组装；
- `app/quality.py`、`app/manuscript_quality.py`：逐章与全书确定性质量门禁；
- `app/providers.py`、`app/llama_client.py`：模型供应商和流式调用；
- `app/writing_skills.py`：供 agent 使用的写作技能注入与约束。

### 前端

- `static/index.html`、`static/app.js`、`static/style.css`：面向非技术作者的项目、规划、写作、记忆、审计和设置界面。

### 终稿与验收脚本

- `scripts/finalize-qince-siliconflow.py`：第58—100章真实终审、哈希校验、断点恢复和事务提交；
- `scripts/audit-qince-foundation.py`：第1—57章严格时间顺序真实模型复审；
- `scripts/finalize-qince-revisions.py`：第46、47、54章返修审计与提交；
- `scripts/normalize-qince-memory-assets.py`：100份记忆资产证据归一、未来信息清理和终局回收；
- `scripts/rebuild-qince-memory.py`：正式库备份、正文哈希保护、1—100章确定性记忆重建和100分门禁；
- `scripts/build-qince-final.py`：可重复生成 Markdown、Windows TXT、JSON 和验收摘要；
- `scripts/run-secure-server.ps1`：掩码读取密钥并仅注入服务进程环境。

### 自动化测试

- `tests/test_narrative_state.py`：证据回写、知情边界、关系别名和历史返修不倒退；
- `tests/test_qince_finalization_assets.py`：100章记忆证据、未来泄漏、终局线索、安全脚本；
- 其他测试覆盖规划、生成、供应商、恢复、知识层、检索、参考资料、前端绑定、云接口和 Windows 启动器。

## 八、安全与恢复

- API 密钥没有写入项目 JSON、SQLite、脚本、审计报告、导出小说或命令行参数；
- 正式项目 `settings.api_key` 已验证为空；
- 启动脚本使用掩码输入，密钥只存在于服务进程环境；
- 所有正式改写前均有 SQLite 在线备份；
- 正文按 SHA-256 防止错误覆盖；
- 逐章审计证据即时落盘，可断点续跑；
- 正文接纳与记忆提交有显式状态，失败可恢复而不会静默丢失。

## 九、最终交付物

- `artifacts/算尽苍生-100章正式终稿.md`：完整 Markdown 正式小说；
- `artifacts/算尽苍生-100章正式终稿.txt`：带 BOM 的 Windows 兼容正式纯文本；
- `artifacts/算尽苍生-100章候选终稿.md/.txt`：与正式终稿哈希一致的构建追溯副本；
- `artifacts/算尽苍生-100章候选终稿验收.md`：全书确定性验收摘要；
- `artifacts/qince-100-chapter-acceptance.json`：100章逐章质量数据；
- `artifacts/算尽苍生-SiliconFlow前57章复审.md` 与对应 JSON：第1—57章真实复审；
- `artifacts/算尽苍生-SiliconFlow最终审计.md` 与对应 JSON：第58—100章真实终审和提交；
- `artifacts/qince-siliconflow-revision-audit.json`：三章返修真实审计；
- `artifacts/qince-formal-memory-rebuild.json`：正式记忆重建与健康报告；
- 本工程报告：最终实现、缺陷修复、证据和文件索引。

## 十、发布后的建议

当前版本达到工程发布标准。下一步不应继续无门槛批量改写正文；更合理的是：

1. 由真人编辑抽样阅读模型建议最集中的章节，确认是否属于真实阅读问题；
2. 若进行第二版精修，每次只改一章，并走“真实审计—事务接纳—历史记忆保护—全书回归”闭环；
3. 为其他长篇复用本流程时，先建立人物、世界规则、伏笔和知情边界，再生成正文；
4. 将100章时间顺序只读复审作为正式发布模板，避免用完成态记忆审计早期章节；
5. 发布前另做人类层面的错别字、排版、版权、书名和平台合规检查，这些不属于模型连续性测试的替代范围。
