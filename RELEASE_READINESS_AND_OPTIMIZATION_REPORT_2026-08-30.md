# InkForge 0.19.1 正式发布能力与后续优化报告

## 1. 当前判定

InkForge 0.19.1 已达到“本地候选版、人工监督写作可用”的工程状态，但还不能标记为“完整云端正式版”。

已经满足：

- 168 项本地自动测试全部通过。
- 正文生成、规划、审校、Canon、证据化记忆事务、上下文预览等主链路已有实现与回归覆盖。
- 第一轮真实 SiliconFlow 测试验证过正文、跨章召回、Canon/OOC 和局部改写能力。
- 0.19.1 修复了第一轮真实测试发现的确定性阻断项。
- 正式数据库在多轮 QA 前后哈希完全一致。

尚未满足：

- 0.19.1 修复后的云端 E2E 尚未取得有效凭据复验；当前环境凭据返回 HTTP 401。
- 还没有 10/30/100 章耐久一致性基准。
- 还没有对所有可选模型完成“写作质量 + 记忆提取 + 大结构闭合率”矩阵测试。
- 自动导演仍需通过故障注入、重启恢复、重复提交和限流测试。

## 2. 0.19.1 本轮已经完成的优化

### 2.1 自动导演分卷契约统一

分卷核心和完整分卷现在共享同一个自适应梗概长度函数：3-6 章短卷为 160 字，其他分卷为 180 字。这样不会再出现“上一阶段合法保存，下一阶段仅因门槛变化而必然失败”的状态。

边界测试覆盖短卷和普通卷，验证核心与详情组合采用完全一致的门槛。

### 2.2 灵感孵化事务化

旧实现要求一次生成两套完整作品方案，并在同一个 JSON 中包含长大纲、全部人物卡和世界书。新实现分成三次受限结构：

1. 生成两套作品方向核心，不含人物和世界资产。
2. 为第一套方向生成 3-5 名人物与 3-5 个世界条目。
3. 为第二套方向生成对应资产。

只有三步全部校验通过才返回结果，并通过 `generation_mode=staged_transaction` 暴露诊断信息。该接口仍不写入当前作品，避免半成品污染。

### 2.3 记忆降级真正缩小

完整章节记忆失败后，不再要求模型用另一个仍然复杂的协议重做全部结构。最小事实包只包含：

- 80-160 字本章因果摘要；
- 最多 4 条带正文原句证据的原子事实；
- 最多 3 条带证据的人物章末状态；
- 一个精简场景结算。

滚动摘要缺失时由本地程序构造；关系、时间线、伏笔等结构宁可暂缺，也不会放宽正文证据门。当前策略仍是“宁可少记，不可乱记”。

### 2.4 前置资产历史门禁

自动导演人物卡、世界书和普通灵感孵化资产在成为高优先级设定前增加了时间边界检查。对明确处于战国、秦统一前的项目，系统会拒绝“始皇帝、皇帝、陛下、龙袍、玉玺”等明显后期称谓或物件，并要求模型定点重做。

这是一道窄而确定的安全线，不冒充完整历史知识库。正式历史题材仍应使用用户提供的时间线、世界书和正典资料。

### 2.5 质量自比较保护

当候选稿已经保存到当前章节后，质量接口不会再拿同一文本作为“旧正文”比较。真实修订稿与不同旧正文仍会进行复用检测。

## 3. 与专业项目相比，正式产品还需要什么

### 3.1 语义检索与多样化召回

GoInk 强调本地语义搜索和 Agent 主动查询；InkOS 使用时间记忆与相关性检索。InkForge 当前的实体、字符/二元词匹配适合确定性基础，但对“那个吊坠”“她第一次撒谎的地方”这类语义回忆仍不够稳。

建议按以下顺序实现：

1. SQLite FTS5：章节、摘要、事实、伏笔、角色知识统一全文索引。
2. 精确实体与别名过滤：角色、地点、物件先缩小候选集。
3. 可选本地/云嵌入召回：只作为候选检索，不直接成为事实。
4. MMR 去重：防止相似章节片段占满上下文。
5. 每次召回记录 query、候选、得分、选中/淘汰原因。

发布指标：人工标注 100 个跨章问题，Top-5 召回率至少 90%，错误硬事实注入率为 0。

参考：[GoInk](https://github.com/sigpanic/goink)、[InkOS](https://github.com/Narcooo/inkos)。

### 3.2 更完整的读者认知和人物认知边界

“系统知道”不等于“人物知道”，更不等于“读者知道”。正式长篇应把知识至少分成：

- 客观事实；
- 人物已确认；
- 人物怀疑；
- 谣言或错误认知；
- 读者已知但人物未知；
- 作者计划但正文尚未发生。

当前项目已经有 knowledge ledger、visibility 和 evidence，但还需要统一查询接口和审校规则，防止不同模块绕过知情边界。

发布指标：设计 30 个“读者知道、主角不知道”“A 知道、B 不知道”的对抗用例，未授权知情泄漏为 0。

### 3.3 可解释上下文位置

NovelAI 把 Memory、Author’s Note、Lorebook 和最近正文放在不同位置，并提供 Context Viewer。InkForge 已经能预览选中区块和预算，下一步需要把以下信息完整显示给普通作者：

- 本次真正送入模型的最终顺序；
- 每个区块的字符/token 估算；
- 为什么被激活；
- 为什么被裁剪；
- 哪些硬规则受保护；
- 当前正文预留了多少输出空间。

还应加入“冻结本次上下文快照”，让一次失败生成可以用完全相同的输入重放。

参考：[NovelAI Story Settings](https://docs.novelai.net/en/text/editor/storysettings/)、[NovelAI Lorebook](https://docs.novelai.net/en/text/lorebook/)。

### 3.4 Diff 审批与设定变更审计

GoInk 的 Diff 预览适合正文修改；InkOS 的 Delta/Reducer 思想适合状态写入。InkForge 应为以下操作提供统一变更预览：

- 正文插入、整章替换、选区重写；
- 人物状态变化；
- 事实新增、替代、停用；
- 关系变化；
- 伏笔推进与回收；
- 世界书和 Canon 修改。

自动导演可以自动提交正文候选，但不能绕过相同的证据化 Memory Service。对硬设定的模型建议默认进入 candidate，只有用户批准或正文证据验证后才能成为 authoritative。

### 3.5 Skills，但不能把 Skills 当记忆

GoInk 和 InkOS 的写作 Skills 适合承载“怎样写”：场景节拍、悬疑钩子、对白潜台词、去 AI 味、历史称谓审校、同人 OOC 审校等。它们不应保存“小说发生了什么”。

推荐三层：

- 内置只读 Skills：系统维护并版本化；
- 用户 Skills：跨作品复用个人写作方法；
- 作品 Skills：只对当前小说生效。

推荐三种激活方式：

- always：短小、始终生效的写作原则；
- auto：按任务、题材、章节状态自动选择；
- manual：用户主动调用。

每次 Skill 运行要记录名称、版本、输入哈希、上下文区块、输出类型和是否允许写状态。Skill 只能提出 Delta，不能直接绕过记忆事务写库。

Skills 建议放到 0.20/0.21，而不是作为当前云端发布阻断项。先确保记忆和自动导演在真实模型下稳定，再扩展方法论入口。

参考：[GoInk Skills](https://github.com/sigpanic/goink/blob/master/README_EN.md)、[Story Skills](https://github.com/danjdewhurst/story-skills)。

### 3.6 耐久测试和模型路由

标称 128k/256k 上下文不能证明长篇一致性。发布前必须建立任务级模型评测：

| 任务 | 主要指标 |
| --- | --- |
| 正文生成 | 硬规则违规率、情节推进、句式疲劳、长度完成率 |
| 记忆提取 | JSON 闭合率、事实召回率、证据通过率、错误写入率 |
| 规划 | 结构完整率、重复卷/章比例、终局提前兑现率 |
| Canon/OOC | 召回率、误报率、无证据指控率 |
| 孵化 | 完成率、方向差异、资产冲突率、总耗时与成本 |

模型路由必须按任务指标选择，而不是只看上下文窗口或一次小 JSON 的速度。

## 4. 推荐发布门禁

### 必须通过（P0/P1）

1. 0.19.1 使用有效 SiliconFlow 凭据完成完整 32 项 E2E，FAIL 为 0。
2. 灵感孵化连续运行 20 次，完整事务成功率至少 95%，无半成品写入。
3. 自动导演完成 3 章、10 章两档测试，应用重启后可续跑。
4. 章节记忆结构成功率至少 95%；错误权威事实写入为 0。
5. 历史/Canon 硬冲突不得进入人物卡、世界书、故事圣经和正文。
6. 所有 Provider 的 401、429、超时、断流和 JSON 截断都有可理解错误和安全恢复。
7. QA 使用隔离数据库，正式数据库哈希保持不变。
8. 密钥扫描无命中，导出文件、数据库、日志、压缩包均不含密钥。

### 发布后建设（P2）

1. FTS5 + 实体过滤 + 可选向量 + MMR 的混合检索。
2. Skills 三层注册表和权限清单。
3. 正文/设定统一 Diff 审批。
4. 30/100 章耐久评测与质量趋势面板。
5. 手机安全访问、登录、TLS、备份与多设备冲突策略。

## 5. 当前唯一外部阻塞

0.19.1 的本地实现和 168 项测试已完成。修复后云端复测连接 SiliconFlow 时返回 HTTP 401，因此不能声称三个真实云端问题已经通过复验。

这次 401 报告只能证明：

- 项目正确从进程环境发现了凭据来源；
- Provider 将上游认证失败准确传回；
- 灵感、规划和自动导演没有把失败输出写成有效云端结果；
- 自动导演在认证失败后安全暂停。

它不能证明：

- 分阶段孵化在真实 Qwen3-8B 上已经闭合；
- 最小记忆恢复在真实 Qwen3-8B 上已经成功；
- 自动导演已经完成三章。

因此正式发布状态仍是：**本地 Release Candidate；云端验收待有效凭据复测。**

## 6. 证据

- 0.19.1 变更说明：`CHANGELOG_0.19.1.md`
- 首轮真实模型报告：`qa/cloud_runs/20260830-010012/QA_REPORT.md`
- 修复后认证失败报告：`qa/cloud_runs/20260830-125827/QA_REPORT.md`
- 首轮专业测试分析：`SILICONFLOW_FULL_QA_REPORT_2026-08-30.md`
- 全项目与外部项目分析：`INKFORGE_PROJECT_COMPARISON_AND_MEMORY_IMPLEMENTATION_REPORT_2026-08-29.md`

正式数据库及测试前备份 SHA-256 均为：

`AF9CAB8FB28F3E7D9B018ECC2AF6FE61A9636610BBCE5110189724AD7EBEF03D`
