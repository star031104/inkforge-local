# 全稿质量架构（v0.15.0）

## 目标

单章写得通顺，不代表长篇成立。本层专门阻止四类跨章问题：原句/段落复用、同一剧情功能循环、分卷终局重复、历史题材被现代抽象术语淹没。

## 三道质量门

1. **规划门**：每卷契约必须声明 `primary_arena`、`time_span`、`irreversible_change`、`character_choice`、`new_story_question`。系统同时比较各卷 goal、ending_state、theme_test 和这些新增字段；非终卷不能提前兑现全书结局。
2. **生成门**：提示词注入上一卷已完成状态、下一卷边界、全书疲劳词和跨章已重复句段。章节必须改变信息、资源、关系、权限、风险或立场中的至少一项。
3. **接纳门**：本地审计把候选与此前全部正文比较。出现结构性重复时，不拿旧候选做同义改写，而是丢弃候选、按同一章末目标重新设计场景。

## 失败策略

- JSON 截断、连接超时等结构故障：沿用 v0.14.0 的重试与检查点恢复。
- 单个普通质量债：记录并把建议注入下一次修订。
- 结构性质量债：新起草，不沿用问题候选。
- 连续 3 次结构性质量债或连续 5 章未达标：在写入当前低质量候选前熔断。此前检查点不丢失，可修订规划后继续。

这一区分避免“一个软问题就停整书”，也避免 `continue_on_quality_debt` 把 100 个同类错误一路写完。

## 全稿体检 API

`POST /api/project/manuscript-health`

请求体使用当前前端项目快照，因此未保存编辑也能体检。返回：

- `duplicate_titles`
- `duplicate_passages`
- `similar_chapters`
- `fatigued_phrases`
- `modern_jargon`
- `volume_progression_issues`
- `recommendations`

项目体检只报告，不会自动删除正文。已有“清空完全重复整章”仍需用户勾选并确认，且会写入版本历史。

## 回归标准

- 单元测试覆盖跨章重复、体检 API、全局疲劳词、重复卷场域和非终卷提前兑现。
- 所有旧测试必须继续通过，保证手动创作、提示词预览、记忆和恢复流程不退化。
