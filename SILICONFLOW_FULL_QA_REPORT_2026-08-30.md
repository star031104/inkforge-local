# InkForge 0.19.0 × SiliconFlow 完整专业测试报告

## 1. 测试结论

本次测试结论为：**核心写作链路可用，长期记忆安全策略有效，但尚未达到“云端全功能发布门禁通过”状态。**

- 自动化与接口基础：通过。
- 六个指定模型的账户可见性、连接与严格 JSON 烟测：全部通过。
- 真实小说写作主链路：通过，包括文风分析、章节规划、世界书激活、正文生成、连续性审计、整章修订、跨章召回、局部重写/扩写、Canon Lock 和 OOC 审校。
- 数据安全：通过。测试使用隔离项目；正式数据库在测试前后 SHA-256 完全一致；API Key 未写入项目、数据库、普通报告或压缩包。
- 完整云端 E2E：32 项中 27 PASS、3 WARN、2 FAIL，因此总结果为 FAIL。

当前版本适合进行人工监督下的真实写作测试；不建议在修复本报告的 P1 问题前，把“灵感一键孵化”和“自动导演无人值守长跑”视为稳定功能。

## 2. 测试环境与范围

- 应用版本：InkForge 0.19.0
- API Schema：34
- 测试日期：2026-08-30（Asia/Shanghai）
- 本地服务：http://127.0.0.1:7860
- 云端服务：https://api.siliconflow.cn/v1
- 完整 E2E 主模型：`Qwen/Qwen3-8B`
- 本地自动测试：161 项
- 云端 E2E：32 项
- 实际写作题材：战国末期秦国粮账调查，带历史称谓、物质条件、人物知情边界等硬约束

覆盖范围包括：

1. 服务健康、Provider 配置、环境变量密钥注入与模型列表。
2. 普通对话、严格 JSON、SSE 流式、非思考正文输出。
3. 多样本文风分析、章节细纲、正文生成、长度修复。
4. 世界书激活、连续性审计、质量门禁、审计修订。
5. 章节记忆提取、严格证据回灌、知识图谱、跨章召回。
6. 选区重写、选区扩写、全稿体检。
7. Canon Profile、Canon Lock、OOC/Canon 审校。
8. 灵感推荐、灵感孵化、全书分层规划、自动导演三章任务。
9. 正式数据库不变性和测试制品密钥扫描。

## 3. 六模型兼容性矩阵

六个用户指定模型均在当前 SiliconFlow 账户的模型列表中，并完成严格 JSON 烟测。

| 模型 | 标称上下文 | 严格 JSON 烟测 | 单次观测耗时 | 工程定位建议 |
| --- | ---: | --- | ---: | --- |
| `Qwen/Qwen2.5-7B-Instruct` | 32k | PASS | 约 2.1 秒 | 低成本短任务、重写、提取；长结构任务需分段 |
| `THUDM/GLM-4-9B-0414` | 32k | PASS | 约 2.6 秒 | 短规划、审校和普通章节辅助 |
| `THUDM/GLM-Z1-9B-0414` | 128k | PASS | 约 8.7 秒 | 复杂分析可用，但需控制延迟 |
| `Qwen/Qwen3-8B` | 128k | PASS | 约 2.8 秒 | 本次主测模型；写作主链路表现良好 |
| `deepseek-ai/DeepSeek-R1-0528-Qwen3-8B` | 128k | PASS | 约 25.8 秒 | 深度规划/审校候选，不宜作为高频交互默认值 |
| `Qwen/Qwen3.5-4B` | 256k | PASS | 约 1.6 秒 | 快速上下文任务候选；仍需做长篇质量基准后再定默认值 |

注意：上述耗时是本次网络和账户条件下的单次烟测观测，不是正式性能基准；“上下文长”也不等于“单次能稳定输出超大 JSON”。本次失败恰好证明，输出结构的大小、字段数量、停止原因与恢复策略比输入上下文上限更关键。

## 4. 结果总览

| 类别 | 结果 | 说明 |
| --- | --- | --- |
| 本地自动测试 | PASS | 161/161，通过时间 7.27 秒 |
| Provider 与模型发现 | PASS | 找到 64 个 chat 模型，目标模型均存在 |
| Chat/JSON/SSE | PASS | 普通请求、严格 JSON、流式非思考输出均正常 |
| 历史小说正文 | PASS | 第一章 966 字、第二章 710 字；未命中测试禁词 |
| 文风/规划/世界书 | PASS | 多样本文风、章节细纲、设定激活正常 |
| 审校与改写 | PASS | 连续性审计、整章修订、局部重写/扩写均完成 |
| Canon/OOC | PASS | Canon Lock 607 字，OOC 审校 95 分并通过 |
| 长期记忆召回 | 部分通过 | 本地摘要与跨章召回可用；结构化事实提取发生降级 |
| 灵感孵化 | FAIL | 大型单体 JSON 两次未闭合，无分段恢复 |
| 全书分层规划 | PASS | 生成 2 卷 |
| 自动导演三章 | FAIL | 在分卷核心/详情契约不一致处安全暂停，0 章完成 |
| 密钥与正式数据安全 | PASS | 未发现密钥落盘；正式数据库哈希未变化 |

## 5. 已验证可用的核心能力

### 5.1 写作链路

真实模型成功完成从文风分析、章节细纲到两章连续正文的完整路径。第一章触发长度修复后达到 966 字；审计修订稿为 764 字；第二章基于前章记忆继续生成 710 字。局部重写与扩写分别生成 317 字和 342 字，说明统一生成接口的主要工作模式均可运行。

历史题材测试设置了“皇帝、始皇帝、现代管理和数字化词汇”等禁区。直接章节生成未命中禁词，世界书中的“秦统一前称谓”和“仓曹物质条件”均被提示词预览正确激活。这证明上下文编排和正文阶段的硬约束注入已经生效。

### 5.2 长期记忆的安全性

本次结构化章节记忆提取虽然失败并降级，但系统没有把不可靠的模型输出写入事实层：事实数为 0，严格证据门禁阻止了记忆污染。同时，本地基础摘要仍被保存，第二章检索到 1 条相关前章记忆。因此当前设计表现为“宁可少记，也不乱记”，这对长篇小说比静默写入幻觉更安全。

知识图谱投影得到 5 个节点、0 条边，也符合严格证据不足时不创建关系的策略。该结果不是完整记忆能力的成功，而是安全降级机制的成功。

### 5.3 Canon 与同人一致性

Canon Profile 成功产生 6 条禁止项和 15 条显式设定；Canon Lock 生成成功；OOC/Canon 审校为 95 分并通过。这说明独立的原作权威层、写作上下文注入和事后审校已形成闭环。

### 5.4 数据与密钥安全

- 正式数据库当前 SHA-256：`AF9CAB8FB28F3E7D9B018ECC2AF6FE61A9636610BBCE5110189724AD7EBEF03D`
- 测试前备份 SHA-256：`AF9CAB8FB28F3E7D9B018ECC2AF6FE61A9636610BBCE5110189724AD7EBEF03D`
- 两者相等，说明完整云端测试没有改动用户正式数据库。
- 测试报告、普通输出文件和压缩包扫描未发现 API Key。
- 项目配置仅记录密钥来源为环境变量，不保存密钥正文。

## 6. 问题与根因分析

### P1 / DIR-001：自动导演的分卷校验契约不一致

**现象**：自动导演成功完成故事骨架、人物名单、4 张人物卡、世界书、故事圣经、分卷契约和分卷核心，随后在“分卷转折与人物弧”阶段暂停，错误为“分卷详细剧情梗概至少需要 180 字”，最终完成章节数为 0。

**根因证据**：

- `validate_director_volume_core` 接受 `synopsis >= 160`。
- 上一步生成的核心梗概在第一次 154 字失败后自动重试并被保存，说明重试版本通过了 160 字门槛。
- 随后的详情组合又调用要求 `synopsis >= 180` 的完整分卷校验。
- 详情阶段本身生成的是 turning points、character arcs 等执行清单，不负责重新扩写已保存的 synopsis，因此 160–179 字的合法核心会在下一阶段必然变成非法输入。

**影响**：这是确定性的流程契约缺陷。模型即使完全按当前核心提示完成，也可能在下一阶段被拒绝；自动导演无法进入章节规划和正文生成。

**建议修复**：将核心与最终分卷的 synopsis 下限统一。推荐核心阶段直接要求并验证 180 字，同时在详情组合前做一次可恢复的核心扩写；或者把最终组合门槛统一为 160。前者更适合长篇质量，但短篇三章应使用按目标章节数自适应的下限。为 159、160、179、180 四个边界增加单元与任务续跑测试。

### P1 / MEM-001：章节记忆的完整与精简恢复均出现 JSON 截断

**现象**：完整记忆提取和精简 AI 恢复都返回未闭合 JSON，最终使用本地基础记忆；结构化事实为 0。

**实现证据**：完整模式 `max_tokens=1900`、`token_ceiling=2400`；精简恢复 `max_tokens=1200`、`token_ceiling=1500`。两次调用都要求同一验证器可接受的多数组结构。对于 8B 模型，精简提示并没有从协议层显著减少必填结构或改为分批抽取。

**影响**：摘要仍可支持近期续写，但人物状态、时间线、关系、伏笔和可核验事实无法稳定沉淀。小说越长，长期一致性越依赖这些结构化记录。

**建议修复**：

1. 把记忆提取拆成“摘要/场景结算”和“事实/人物/关系/时间线”两次小 JSON。
2. 根据 `finish_reason=length` 直接进入更小协议，而不是用相近的大协议重试。
3. 精简恢复只要求 `summary`、`story_so_far`、`facts`，其他字段按后续任务异步补齐。
4. 保留当前证据门禁，严禁为了提高事实数量而放宽 source quote/evidence 校验。
5. 增加真实 8B 模型的 500、1000、2000、5000 中文字章节矩阵测试，统计结构闭合率、事实召回率和错误写入率。

### P1 / INC-001：灵感孵化是过大的单体结构，缺少分段恢复

**现象**：灵感孵化在主请求与结构恢复后仍得到未闭合 JSON，接口安全返回 502，原始灵感未被修改。

**根因证据**：单次请求要求固定 2 套完整方案；每套包含定位、命题、读者承诺、冲突、故事引擎、大纲、作者意图、硬规则、结局、开篇、第一阶段、人物和世界设定等大量字段，使用 `max_tokens=5600`。失败分支没有精简协议或分步继续，只提示重试。

**影响**：用户最先接触的“一句话开书”路径不稳定；重复点击仍可能产生同类截断，并增加成本。

**建议修复**：采用四段事务式孵化：先返回两套 300–600 字方向摘要；用户或系统选定一套；再分别生成人物名单、逐人物卡、世界书；最后生成故事圣经。自动导演已经采用近似分段模式且其前半程成功，普通孵化接口应复用这套小结构流水线。每一步使用幂等草稿 ID，全部成功后才提交到项目。

### P2 / HIST-001：自动导演前置资产缺少历史硬规则的确定性复核

**现象**：直接正文链路遵守了秦统一前称谓，但自动导演生成的“秦王嬴政”人物卡包含“始皇帝”“陛下”“朕”“玄色龙袍”“玉玺”“帝国统一”等明显跨期或高风险表述；其他人物卡还出现“幞头”等时代疑点。生成结果同时声明“称谓与历史背景需准确”，形成内部自相矛盾。

**影响**：这些人物卡和世界书会成为高权重长期上下文，后续可能反向污染正文。即使正文生成器本身能遵守世界书，也可能是在忠实执行错误设定。

**建议修复**：在人物卡、世界书、故事圣经写入前运行 Canon/历史禁词与时间边界校验；检测到硬冲突时只重写冲突字段，不整份重做。将项目 `book_rules`、constant world entries 和历史节点作为前置资产生成阶段的强制 known context，并让保存操作通过与正文相同的严格证据/冲突门禁。

### P2 / QA-001：第一章质量门禁产生自比较误报

**现象**：质量门禁报告“候选稿原样复用了已有正文中的 26 个完整长句”，评分 86、结论 revise。

**根因证据**：测试脚本先执行 `chapter["content"] = draft`，再把同一个 `draft` 传给 `/api/chapter/quality`。接口把 `chapter.content` 作为 `source_tail`，因此候选稿与自身比较，26 个重复长句是测试编排造成的误报，不代表模型真的抄写旧正文。

**影响**：本次质量分不能用于评价第一章复用率；“沈砚将算”的句式疲劳仍是独立有效提示。真实前端若在检查前已经把草稿同步回 chapter.content，也可能复现相同问题，需要核对调用时序。

**建议修复**：测试脚本应保留生成前正文并传给质量接口；接口层增加防御：当 `draft` 与 `chapter.content` 完全相同或高度相似时，不把它作为 source_tail。补充“空正文、新草稿、已保存同稿、基于旧稿续写”四种用例。

## 7. 修复优先级与工程实施顺序

### 第一批：阻断项

1. 修复 DIR-001，统一分卷核心和完整分卷的梗概契约，并增加边界测试。
2. 将灵感孵化改为分阶段事务，复用自动导演已验证成功的 brief/cast/characters/world 流程。
3. 将章节记忆恢复协议真正缩小或拆分，记录 `finish_reason`、输出字符数和恢复路径，但不得记录密钥或完整敏感提示词。

### 第二批：长期质量项

1. 给所有自动导演前置资产增加历史/Canon 冲突校验。
2. 修复质量门禁的候选稿自比较问题。
3. 增加模型路由：短 JSON/局部改写使用快速模型，复杂规划使用更稳模型；路由依据任务类型和失败历史，而不是只依据上下文窗口。

### 第三批：发布级验证

1. 六模型分别执行记忆提取、孵化、分卷核心/详情，而不仅是严格 JSON 烟测。
2. 自动导演连续完成 3 章，再执行 10 章耐久测试。
3. 每章验收：正文质量、硬规则、状态结算、证据事实、跨章召回、关系图和伏笔状态。
4. 故障注入：429、超时、断流、JSON 截断、应用重启、任务恢复、重复提交。
5. 发布门禁目标：P1 为 0；E2E FAIL 为 0；记忆结构成功率至少 95%；硬规则冲突写入为 0；正式数据库不被 QA 修改。

## 8. 推荐模型使用策略

当前测试只能证明六个模型“可连接且能输出严格小 JSON”，不能仅据此断言某个模型最适合整本小说。建议：

- 默认交互、选区改写、短提取：先评估 `Qwen/Qwen3.5-4B` 与 `Qwen/Qwen2.5-7B-Instruct`，以成本、延迟和中文质量做 A/B。
- 正文生成和常规规划：当前可继续使用 `Qwen/Qwen3-8B`；本次正文与 Canon 链路已验证。
- 高风险结构规划或二次审校：可评估 `THUDM/GLM-Z1-9B-0414` 与 `deepseek-ai/DeepSeek-R1-0528-Qwen3-8B`，但后者本次延迟明显更高。
- 所有模型：大型 JSON 必须拆分；不能因为标称 128k/256k 上下文而取消结构化恢复和事务提交。

## 9. 验收判定

本次版本判定：

- **本地回归：通过**
- **SiliconFlow 接入：通过**
- **人工监督写作主链路：通过**
- **长期记忆安全降级：通过**
- **长期记忆完整抽取：未通过**
- **灵感孵化：未通过**
- **自动导演无人值守：未通过**
- **正式数据与密钥安全：通过**
- **综合发布门禁：未通过**

建议修复 DIR-001、MEM-001、INC-001 后，用同一隔离测试项目重新执行完整 E2E；只有 32 项中 FAIL 清零、WARN 均有明确可接受原因时，才将本版本标记为完整云端验收通过。

## 10. 证据索引

- 完整机器可读结果：`qa/cloud_runs/20260830-010012/qa-report.json`
- 本次运行简报：`qa/cloud_runs/20260830-010012/QA_REPORT.md`
- 可归档压缩包：`qa/cloud_runs/InkForge_Cloud_QA_20260830-010012.zip`
- 真实写作样稿：`qa/cloud_runs/20260830-010012/outputs/`
- 云端完整测试程序：`scripts/cloud_full_e2e.py`
- 自动导演分卷校验：`app/main.py` 的 `validate_director_volume_expansion`、`validate_director_volume_core`
- 章节记忆恢复：`app/main.py` 的章节记忆提取路由
- 灵感孵化：`app/main.py` 的 `/api/incubator`
- 质量门禁：`app/main.py` 的 `/api/chapter/quality`

本报告不包含 API Key；测试密钥仅在本次测试进程环境中使用，未作为项目配置或持久记忆保存。
