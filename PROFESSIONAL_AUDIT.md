# 砚火专业化审计基线（v0.16.0）

本文件记录当前产品边界、与同类开源项目的能力对照、已经落实的质量门，以及后续改动不得破坏的基线。它不是宣传清单，而是维护和验收依据。

## 产品边界

砚火面向 Windows、单作者、单个本地 llama.cpp 服务和约 8GB 显存设备。它优先保证：数据留在本机、长篇结构可审、模型失败可恢复、作者可以在任意阶段接管。它不试图复刻需要多智能体集群、云向量数据库或团队协作权限系统的完整出版平台。

## 开源项目对照

| 能力 | InkOS | AI-Novel-Writing-Assistant | Long-Novel-GPT | 砚火 v0.16.0 |
|---|---|---|---|---|
| 从灵感到整本书 | 自主流水线与人工门 | 自动导演与任务中心 | 以层级扩写为主 | 持久化一键自动导演，可暂停恢复 |
| 分层规划 | 规划、编排、章节意图 | 书级、卷级、章节任务 | 大纲、章节、正文 | 故事圣经、分卷蓝图、逐章路线、执行计划 |
| 长期一致性 | truth/state 文件与审计 | 状态回灌、RAG、审计修复 | 相关片段检索 | 结构化记忆、滚动摘要、世界书、相关检索、写后回灌 |
| 文风 | 风格指纹与审计 | 写作配置与审校 | 参考文本扩写 | LLM 风格卡、动态样例、统计偏移检查 |
| 恢复能力 | 状态快照与恢复 | 检查点和失败暂停 | 编辑过程保存 | 全项目历史、单章快照、JSON 导入、SQLite 轮换备份 |
| 本地小模型容错 | 状态修复 | 重试与停机规则 | 分层降低单次复杂度 | JSON 闭合早停、质量门重试、保守降级、质量债务 |

## 当前专业质量门

1. 全书规划必须覆盖计划章节数；分卷数量、章数总和、字段完整性和相邻卷重复度不合格时不得覆盖旧规划。
2. 分卷按小批次拆章；抽象模板章名、重复标题、近似目标、提前兑现卷末状态和缺字段会触发整批重写。
3. 正文只在已有章节计划、权威事实、人物知识边界、相关世界书和检索记忆下生成；提示词为输出预留预算并记录裁剪轨迹。
4. 候选稿先进入草稿区。接纳前可执行本地检查和 AI 连续性审计；自动导演最多按配置修订，仍不达标则暂停或留下可追踪质量债务。
5. 记忆回写只接受已有角色和可验证的正文证据；事实带来源、置信度、可见性、有效区间与替代关系，人物知情带获得方式，关系变化独立记账。线索采用 open/progressing/deferred/ready/closed 五态生命周期；没有实际兑现与证据时不得关闭。异常不会静默写入，会生成章节警告和结算单。
6. 自动导演把开书拆为故事骨架、人物名单、逐张人物卡和世界书；每张人物卡与后续规划、分卷、章节计划、章节接纳都保存检查点。应用重启后运行中任务转为暂停，不会假装仍在执行。
7. 删除作品和启动整书导演前自动备份数据库；项目 JSON 可以导回新副本；手动/生成接纳保存前保留单章快照。
8. 人物永久核心与章节动态状态分离；对白示例只用于声音校准，世界条目按触发条件、作用域、互斥组和预算确定性激活。
9. 接纳正文后只把可在正文中验证的显著描写写入去重账本；后续生成与审计避免原样复用，不把必要事实重复误判为问题。
10. 自动导演的硬故障和软质量问题分流：只有连接持续失败或四轮都没有完整结构时暂停；重复、岗位偏离、时代语言等质量问题先定向改写，最终以最佳完整候选继续并留下规划债务。
11. 恢复不是盲信旧检查点。每卷继续前按当前质量门顺序复核，发现问题时保留有效前缀、记录撤回历史并从首个坏路线重建。
12. 规划债务、正文质量债务和恢复事件分别保存并在任务界面展示；降级结果不会冒充完整 AI 结果，也不会静默消失。

## 验收矩阵

- Python 编译必须通过。
- 全部自动测试必须通过。
- `node --check static/app.js` 必须通过。
- `/api/health` 的 schema 版本必须与前端要求一致。
- 导出弹窗必须同时出现 JSON、Markdown、JSON 导入和数据库备份入口。
- 项目体检必须报告重复正文、缺摘要、未细化章节、质量债务、开放线索、逾期伏笔、无兑现关闭、知情来源缺失和待确认状态。
- 浏览器控制台不得出现由应用代码引发的错误。

## 有意保留的边界

- 纯本地小模型无法保证事实判断百分之百正确，审计分数是风险提示，不是出版证明。
- 当前检索不依赖嵌入模型，采用中文词项、相关度、类型配额和相似内容抑制；这是 8GB 单模型环境下的速度与召回折中。
- SQLite 适合单作者本地工作，不提供多人同时编辑、细粒度权限或网络同步。
- 自动导演采用串行调用以避免显存争抢；章节越多，总耗时越长。

在上述产品边界内，v0.16 已完成从“能写”到“可规划、可审计、可恢复、可追踪、可基于证据长期维持人物、世界、关系和暗线一致性”的专业闭环。新增功能若不能说明它改善了创作质量、恢复能力或作者控制权，不应继续堆入主界面。
